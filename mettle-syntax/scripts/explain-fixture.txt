
-- optimization report: explain_demo.mettle ----------
  saxpy (loop @ line 12): vectorized -> vfmadd231ps, 8-wide float32 affine map
  with_call (loop @ line 19): vectorized -> vfmadd231ps, 8-wide float32 affine map
  with_call (call to `scale` @ line 20): inlined
  sum_bytes (loop @ line 27): NOT vectorized
      \_ reason: this is a byte-sum loop, but the vpsadbw kernel accumulates into int64 and this loop's accumulator is narrower
      \_ fix: declare the accumulator as int64 (sum bytes as `total = total + (int64)data[i]`)
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpsadbw, 32-wide byte sum (AVX2)
  matvec (loop @ line 36): NOT vectorized
      \_ reason: the body contains a nested loop (line 38), and only innermost loops are vectorized; the inner loop did not vectorize either -- see its remark
  matvec (loop @ line 38): NOT vectorized
      \_ reason: this is a float multiply-accumulate (dot-product shape), but no kernel matched its address pattern -- the bases must be plain pointers indexed by the loop counter (base[i])
      \_ fix: hoist invariant index math into a pointer before the loop (e.g. `var row: float32* = &m[r * cols];` then `row[c]`)
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vfmadd231ps, 8-wide float32 FMA dot product
  tiny_fixed (loop @ line 47): fully unrolled (8 iterations, constant trip count)
  apply_bias (loop @ line 61): NOT vectorized
      \_ reason: no vectorizer recognized this loop's shape
      \_ fix: vectorizable shapes are unit-stride accesses (a[i], not a[i*k]) over int8/int32/float32/float64 with a straight-line body: maps (a[i] = expr), '+' reductions (s = s + expr), and dot products
  apply_bias (call to `bias` @ line 62): inlined
  sum_ints (loop @ line 76): NOT vectorized
      \_ reason: this loop sums int32 values into `s`, but the int32 reduction kernel accumulates into int64 (eight lanes are summed without overflow only there) and `s` is int32
      \_ fix: declare the accumulator `s` as int64
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpaddd, 8-wide int32 sum (AVX2)
  sum_shorts (loop @ line 86): NOT vectorized
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 elements
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpaddd, 8-wide int32 sum (AVX2)
  apply_damp (loop @ line 99): vectorized -> vfmadd231ps, 8-wide float32 affine map
  apply_damp (call to `damp` @ line 100): inlined
  skew (loop @ line 109): NOT vectorized
      \_ reason: this is a float multiply-accumulate (dot-product shape), but no kernel matched its address pattern -- the bases must be plain pointers indexed by the loop counter (base[i])
      \_ fix: none via hoisting -- re-checked: the index half that is not the loop counter changes every iteration, so it cannot be hoisted out; this access is genuinely non-unit-stride
  chain (call to `step1` @ line 123): inlined
  chain (call to `step1` @ line 123): inlined
  chain (call to `step1` @ line 123): inlined
  main (loop @ line 132): NOT vectorized
      \_ reason: the loop only writes (a fill/init pattern, no array reads); the recognizers cover maps, reductions, and dot products over loaded data, and no constant-fill kernel exists yet
  main (loop @ line 139): NOT vectorized
      \_ reason: the loop only writes (a fill/init pattern, no array reads); the recognizers cover maps, reductions, and dot products over loaded data, and no constant-fill kernel exists yet
  main (call to `saxpy` @ line 143): inlined
  main (call to `with_call` @ line 144): NOT inlined
      \_ reason: the callee's loops were vectorized into SIMD kernels after inlining ran; it stays a real call (the kernel runs the same either way)
  main (call to `matvec` @ line 145): inlined
  main (loop @ line 148): NOT vectorized
      \_ reason: the loop only writes (a fill/init pattern, no array reads); the recognizers cover maps, reductions, and dot products over loaded data, and no constant-fill kernel exists yet
  main (call to `sum_bytes` @ line 149): inlined
  main (call to `apply_bias` @ line 149): inlined
  main (loop @ line 152): NOT vectorized
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 (or int8 if the values fit)
  main (call to `sum_ints` @ line 153): inlined
  main (call to `sum_shorts` @ line 153): inlined
  main (call to `apply_damp` @ line 154): inlined
  main (call to `norm2` @ line 155): inlined
  main (call to `chain` @ line 155): inlined
  main (call to `skew` @ line 155): inlined
  main (call to `opaque` @ line 156): inlined
  main (call to `tiny_fixed` @ line 156): inlined


-- backend report: explain_demo.mettle ----------
  14/18 functions reaching codegen (after inlining) compiled with the register-allocating backend
  35.8% of the program's 491 optimized IR instructions are in register-allocated code

  4 functions use baseline (spill-everything) codegen, grouped by cause, largest first:

  contains the SIMD kernel `simd_affine_map_f32`, which the register allocator doesn't cover yet (4 functions, 315 instructions):
      \_ consequence: the kernel itself runs at full vector speed; only the scalar code around it keeps values on the stack
      \_ fix: nothing for small functions; if a LARGE function mixes a kernel with hot scalar code, move the kernel loop into its own small function so the scalar part keeps the register allocator
      \_ main (300), apply_damp (5), saxpy (5), with_call (5)

Note: transitional runtime objects detected at 'G:\Projects\Mettle\bin\runtime'. Use --build to assemble and link them automatically when needed (most programs link nothing from this directory).
