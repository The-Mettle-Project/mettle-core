
-- optimization report: explain_contracts_demo.mettle ----------
  apply_bias (loop @ line 17): NOT vectorized
      \_ reason: no vectorizer recognized this loop's shape
      \_ fix: vectorizable shapes are unit-stride accesses (a[i], not a[i*k]) over int8/int32/float32/float64 with a straight-line body: maps (a[i] = expr), '+' reductions (s = s + expr), and dot products
  apply_bias (call to `bias` @ line 18): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here
  apply_bias (call to `bias` @ line 18): hoisted out of the loop (runs once, not every iteration)
      \_ reason: `@pure` + loop-invariant arguments enable loop-invariant code motion
  norm2 (function @ line 24): verified @noalloc -- allocation-free on every reachable path
  apply_damp (loop @ line 35): NOT vectorized
      \_ reason: each iteration calls `damp`; loops vectorize only after every call in the body has been inlined away
      \_ fix: remove `@noinline` from `damp` (it blocks this loop's vectorization), or hoist the call out of the loop
      \_ verified: simulated removing `@noinline` from `damp` and re-ran the inliner and the optimizer: this loop then vectorizes -> vfmadd231ps, 8-wide float32 affine map
  apply_damp (call to `damp` @ line 36): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here
  chain (call to `step1` @ line 47): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here
  chain (call to `step1` @ line 47): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here
  chain (call to `step1` @ line 47): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here
  sum_shorts_narrow (loop @ line 54): NOT vectorized
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 elements and declare the accumulator as int64
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpaddd, 8-wide int32 sum (AVX2)
  main (loop @ line 65): NOT vectorized
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 (or int8 if the values fit)
  main (call to `apply_damp` @ line 71): inlined
  main (call to `apply_bias` @ line 72): inlined
  main (call to `sum_shorts_narrow` @ line 72): inlined
  main (call to `norm2` @ line 73): inlined
  main (call to `chain` @ line 73): NOT inlined
      \_ reason: the callee makes more than 2 calls of its own (inlining glue functions bloats callers without runtime gain)
      \_ fix: mark the callee @inline to override the call-count cap
      \_ verified: re-checked with @inline pretend-applied: the structural guards pass, so this call will inline
  main (call to `opaque` @ line 74): NOT inlined
      \_ reason: the callee is marked @noinline
      \_ fix: remove @noinline if inlining is wanted here


-- backend report: explain_contracts_demo.mettle ----------
  7/10 functions reaching codegen (after inlining) compiled with the register-allocating backend
  29.3% of the program's 174 optimized IR instructions are in register-allocated code

  3 functions use baseline (spill-everything) codegen, grouped by cause, largest first:

  contains a call form the register allocator doesn't support yet (3 functions, 123 instructions):
      \_ consequence: every value in the function is kept on the stack instead of in registers
      \_ main (103), apply_damp (16), chain (4)

Note: transitional runtime objects detected at 'G:\Projects\Mettle\bin\runtime'. Use --build to assemble and link them automatically when needed (most programs link nothing from this directory).
