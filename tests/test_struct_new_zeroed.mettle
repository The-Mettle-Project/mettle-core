// Struct from new — zeroed correctly
struct Box {
  x: int32;
  y: int32;
  z: int64;
}

fn main() -> int32 {
  var p: Box* = new Box;
  if (p->x == 0 && p->y == 0 && p->z == 0) {
    return 1;
  }
  return 0;
}
