import "std/io";

fn inner_defer() { puts(cstr("INNER\n")); }
fn after_block() { puts(cstr("AFTER\n")); }
fn outer_defer() { puts(cstr("OUTER\n")); }

fn main() -> int32 {
  defer outer_defer();

  {
    defer inner_defer();
  }

  after_block();
  return 0;
}
