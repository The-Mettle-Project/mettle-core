struct S0 {
  a: int64;
  b: int64;
  c: int64;
}

struct S1 {
  a: int64;
  b: int64;
  c: int64;
}

struct S2 {
  a: int64;
  b: int64;
  c: int64;
}

struct S3 {
  a: int64;
  b: int64;
  c: int64;
}

struct S4 {
  a: int64;
  b: int64;
  c: int64;
}

struct S5 {
  a: int64;
  b: int64;
  c: int64;
}

struct S6 {
  a: int64;
  b: int64;
  c: int64;
}

struct S7 {
  a: int64;
  b: int64;
  c: int64;
}

struct S8 {
  a: int64;
  b: int64;
  c: int64;
}

struct S9 {
  a: int64;
  b: int64;
  c: int64;
}

struct S10 {
  a: int64;
  b: int64;
  c: int64;
}

struct S11 {
  a: int64;
  b: int64;
  c: int64;
}

struct S12 {
  a: int64;
  b: int64;
  c: int64;
}

struct S13 {
  a: int64;
  b: int64;
  c: int64;
}

struct S14 {
  a: int64;
  b: int64;
  c: int64;
}

struct S15 {
  a: int64;
  b: int64;
  c: int64;
}

fn compute0(x: int64, y: int64) -> int64;
fn compute1(x: int64, y: int64) -> int64;
fn compute2(x: int64, y: int64) -> int64;
fn compute3(x: int64, y: int64) -> int64;
fn compute4(x: int64, y: int64) -> int64;
fn compute5(x: int64, y: int64) -> int64;
fn compute6(x: int64, y: int64) -> int64;
fn compute7(x: int64, y: int64) -> int64;
fn compute8(x: int64, y: int64) -> int64;
fn compute9(x: int64, y: int64) -> int64;
fn compute10(x: int64, y: int64) -> int64;
fn compute11(x: int64, y: int64) -> int64;
fn compute12(x: int64, y: int64) -> int64;
fn compute13(x: int64, y: int64) -> int64;
fn compute14(x: int64, y: int64) -> int64;
fn compute15(x: int64, y: int64) -> int64;
fn compute16(x: int64, y: int64) -> int64;
fn compute17(x: int64, y: int64) -> int64;
fn compute18(x: int64, y: int64) -> int64;
fn compute19(x: int64, y: int64) -> int64;
fn compute20(x: int64, y: int64) -> int64;
fn compute21(x: int64, y: int64) -> int64;
fn compute22(x: int64, y: int64) -> int64;
fn compute23(x: int64, y: int64) -> int64;
fn compute24(x: int64, y: int64) -> int64;
fn compute25(x: int64, y: int64) -> int64;
fn compute26(x: int64, y: int64) -> int64;
fn compute27(x: int64, y: int64) -> int64;
fn compute28(x: int64, y: int64) -> int64;
fn compute29(x: int64, y: int64) -> int64;
fn compute30(x: int64, y: int64) -> int64;
fn compute31(x: int64, y: int64) -> int64;
fn compute32(x: int64, y: int64) -> int64;
fn compute33(x: int64, y: int64) -> int64;
fn compute34(x: int64, y: int64) -> int64;
fn compute35(x: int64, y: int64) -> int64;
fn compute36(x: int64, y: int64) -> int64;
fn compute37(x: int64, y: int64) -> int64;
fn compute38(x: int64, y: int64) -> int64;
fn compute39(x: int64, y: int64) -> int64;
fn compute40(x: int64, y: int64) -> int64;
fn compute41(x: int64, y: int64) -> int64;
fn compute42(x: int64, y: int64) -> int64;
fn compute43(x: int64, y: int64) -> int64;
fn compute44(x: int64, y: int64) -> int64;
fn compute45(x: int64, y: int64) -> int64;
fn compute46(x: int64, y: int64) -> int64;
fn compute47(x: int64, y: int64) -> int64;
fn compute48(x: int64, y: int64) -> int64;
fn compute49(x: int64, y: int64) -> int64;
fn compute50(x: int64, y: int64) -> int64;
fn compute51(x: int64, y: int64) -> int64;
fn compute52(x: int64, y: int64) -> int64;
fn compute53(x: int64, y: int64) -> int64;
fn compute54(x: int64, y: int64) -> int64;
fn compute55(x: int64, y: int64) -> int64;
fn compute56(x: int64, y: int64) -> int64;
fn compute57(x: int64, y: int64) -> int64;
fn compute58(x: int64, y: int64) -> int64;
fn compute59(x: int64, y: int64) -> int64;
fn compute60(x: int64, y: int64) -> int64;
fn compute61(x: int64, y: int64) -> int64;
fn compute62(x: int64, y: int64) -> int64;
fn compute63(x: int64, y: int64) -> int64;
fn compute64(x: int64, y: int64) -> int64;
fn compute65(x: int64, y: int64) -> int64;
fn compute66(x: int64, y: int64) -> int64;
fn compute67(x: int64, y: int64) -> int64;
fn compute68(x: int64, y: int64) -> int64;
fn compute69(x: int64, y: int64) -> int64;
fn compute70(x: int64, y: int64) -> int64;
fn compute71(x: int64, y: int64) -> int64;
fn compute72(x: int64, y: int64) -> int64;
fn compute73(x: int64, y: int64) -> int64;
fn compute74(x: int64, y: int64) -> int64;
fn compute75(x: int64, y: int64) -> int64;
fn compute76(x: int64, y: int64) -> int64;
fn compute77(x: int64, y: int64) -> int64;
fn compute78(x: int64, y: int64) -> int64;
fn compute79(x: int64, y: int64) -> int64;
fn compute80(x: int64, y: int64) -> int64;
fn compute81(x: int64, y: int64) -> int64;
fn compute82(x: int64, y: int64) -> int64;
fn compute83(x: int64, y: int64) -> int64;
fn compute84(x: int64, y: int64) -> int64;
fn compute85(x: int64, y: int64) -> int64;
fn compute86(x: int64, y: int64) -> int64;
fn compute87(x: int64, y: int64) -> int64;
fn compute88(x: int64, y: int64) -> int64;
fn compute89(x: int64, y: int64) -> int64;
fn compute90(x: int64, y: int64) -> int64;
fn compute91(x: int64, y: int64) -> int64;
fn compute92(x: int64, y: int64) -> int64;
fn compute93(x: int64, y: int64) -> int64;
fn compute94(x: int64, y: int64) -> int64;
fn compute95(x: int64, y: int64) -> int64;
fn compute96(x: int64, y: int64) -> int64;
fn compute97(x: int64, y: int64) -> int64;
fn compute98(x: int64, y: int64) -> int64;
fn compute99(x: int64, y: int64) -> int64;
fn compute100(x: int64, y: int64) -> int64;
fn compute101(x: int64, y: int64) -> int64;
fn compute102(x: int64, y: int64) -> int64;
fn compute103(x: int64, y: int64) -> int64;
fn compute104(x: int64, y: int64) -> int64;
fn compute105(x: int64, y: int64) -> int64;
fn compute106(x: int64, y: int64) -> int64;
fn compute107(x: int64, y: int64) -> int64;
fn compute108(x: int64, y: int64) -> int64;
fn compute109(x: int64, y: int64) -> int64;
fn compute110(x: int64, y: int64) -> int64;
fn compute111(x: int64, y: int64) -> int64;
fn compute112(x: int64, y: int64) -> int64;
fn compute113(x: int64, y: int64) -> int64;
fn compute114(x: int64, y: int64) -> int64;
fn compute115(x: int64, y: int64) -> int64;
fn compute116(x: int64, y: int64) -> int64;
fn compute117(x: int64, y: int64) -> int64;
fn compute118(x: int64, y: int64) -> int64;
fn compute119(x: int64, y: int64) -> int64;
fn compute120(x: int64, y: int64) -> int64;
fn compute121(x: int64, y: int64) -> int64;
fn compute122(x: int64, y: int64) -> int64;
fn compute123(x: int64, y: int64) -> int64;
fn compute124(x: int64, y: int64) -> int64;
fn compute125(x: int64, y: int64) -> int64;
fn compute126(x: int64, y: int64) -> int64;
fn compute127(x: int64, y: int64) -> int64;
fn compute128(x: int64, y: int64) -> int64;
fn compute129(x: int64, y: int64) -> int64;
fn compute130(x: int64, y: int64) -> int64;
fn compute131(x: int64, y: int64) -> int64;
fn compute132(x: int64, y: int64) -> int64;
fn compute133(x: int64, y: int64) -> int64;
fn compute134(x: int64, y: int64) -> int64;
fn compute135(x: int64, y: int64) -> int64;
fn compute136(x: int64, y: int64) -> int64;
fn compute137(x: int64, y: int64) -> int64;
fn compute138(x: int64, y: int64) -> int64;
fn compute139(x: int64, y: int64) -> int64;
fn compute140(x: int64, y: int64) -> int64;
fn compute141(x: int64, y: int64) -> int64;
fn compute142(x: int64, y: int64) -> int64;
fn compute143(x: int64, y: int64) -> int64;
fn compute144(x: int64, y: int64) -> int64;
fn compute145(x: int64, y: int64) -> int64;
fn compute146(x: int64, y: int64) -> int64;
fn compute147(x: int64, y: int64) -> int64;
fn compute148(x: int64, y: int64) -> int64;
fn compute149(x: int64, y: int64) -> int64;
fn compute150(x: int64, y: int64) -> int64;
fn compute151(x: int64, y: int64) -> int64;
fn compute152(x: int64, y: int64) -> int64;
fn compute153(x: int64, y: int64) -> int64;
fn compute154(x: int64, y: int64) -> int64;
fn compute155(x: int64, y: int64) -> int64;
fn compute156(x: int64, y: int64) -> int64;
fn compute157(x: int64, y: int64) -> int64;
fn compute158(x: int64, y: int64) -> int64;
fn compute159(x: int64, y: int64) -> int64;
fn compute160(x: int64, y: int64) -> int64;
fn compute161(x: int64, y: int64) -> int64;
fn compute162(x: int64, y: int64) -> int64;
fn compute163(x: int64, y: int64) -> int64;
fn compute164(x: int64, y: int64) -> int64;
fn compute165(x: int64, y: int64) -> int64;
fn compute166(x: int64, y: int64) -> int64;
fn compute167(x: int64, y: int64) -> int64;
fn compute168(x: int64, y: int64) -> int64;
fn compute169(x: int64, y: int64) -> int64;
fn compute170(x: int64, y: int64) -> int64;
fn compute171(x: int64, y: int64) -> int64;
fn compute172(x: int64, y: int64) -> int64;
fn compute173(x: int64, y: int64) -> int64;
fn compute174(x: int64, y: int64) -> int64;
fn compute175(x: int64, y: int64) -> int64;
fn compute176(x: int64, y: int64) -> int64;
fn compute177(x: int64, y: int64) -> int64;
fn compute178(x: int64, y: int64) -> int64;
fn compute179(x: int64, y: int64) -> int64;
fn compute180(x: int64, y: int64) -> int64;
fn compute181(x: int64, y: int64) -> int64;
fn compute182(x: int64, y: int64) -> int64;
fn compute183(x: int64, y: int64) -> int64;
fn compute184(x: int64, y: int64) -> int64;
fn compute185(x: int64, y: int64) -> int64;
fn compute186(x: int64, y: int64) -> int64;
fn compute187(x: int64, y: int64) -> int64;
fn compute188(x: int64, y: int64) -> int64;
fn compute189(x: int64, y: int64) -> int64;
fn compute190(x: int64, y: int64) -> int64;
fn compute191(x: int64, y: int64) -> int64;
fn compute192(x: int64, y: int64) -> int64;
fn compute193(x: int64, y: int64) -> int64;
fn compute194(x: int64, y: int64) -> int64;
fn compute195(x: int64, y: int64) -> int64;
fn compute196(x: int64, y: int64) -> int64;
fn compute197(x: int64, y: int64) -> int64;
fn compute198(x: int64, y: int64) -> int64;
fn compute199(x: int64, y: int64) -> int64;
fn compute200(x: int64, y: int64) -> int64;
fn compute201(x: int64, y: int64) -> int64;
fn compute202(x: int64, y: int64) -> int64;
fn compute203(x: int64, y: int64) -> int64;
fn compute204(x: int64, y: int64) -> int64;
fn compute205(x: int64, y: int64) -> int64;
fn compute206(x: int64, y: int64) -> int64;
fn compute207(x: int64, y: int64) -> int64;
fn compute208(x: int64, y: int64) -> int64;
fn compute209(x: int64, y: int64) -> int64;
fn compute210(x: int64, y: int64) -> int64;
fn compute211(x: int64, y: int64) -> int64;
fn compute212(x: int64, y: int64) -> int64;
fn compute213(x: int64, y: int64) -> int64;
fn compute214(x: int64, y: int64) -> int64;
fn compute215(x: int64, y: int64) -> int64;
fn compute216(x: int64, y: int64) -> int64;
fn compute217(x: int64, y: int64) -> int64;
fn compute218(x: int64, y: int64) -> int64;
fn compute219(x: int64, y: int64) -> int64;
fn compute220(x: int64, y: int64) -> int64;
fn compute221(x: int64, y: int64) -> int64;
fn compute222(x: int64, y: int64) -> int64;
fn compute223(x: int64, y: int64) -> int64;
fn compute224(x: int64, y: int64) -> int64;
fn compute225(x: int64, y: int64) -> int64;
fn compute226(x: int64, y: int64) -> int64;
fn compute227(x: int64, y: int64) -> int64;
fn compute228(x: int64, y: int64) -> int64;
fn compute229(x: int64, y: int64) -> int64;
fn compute230(x: int64, y: int64) -> int64;
fn compute231(x: int64, y: int64) -> int64;
fn compute232(x: int64, y: int64) -> int64;
fn compute233(x: int64, y: int64) -> int64;
fn compute234(x: int64, y: int64) -> int64;
fn compute235(x: int64, y: int64) -> int64;
fn compute236(x: int64, y: int64) -> int64;
fn compute237(x: int64, y: int64) -> int64;
fn compute238(x: int64, y: int64) -> int64;
fn compute239(x: int64, y: int64) -> int64;
fn compute240(x: int64, y: int64) -> int64;
fn compute241(x: int64, y: int64) -> int64;
fn compute242(x: int64, y: int64) -> int64;
fn compute243(x: int64, y: int64) -> int64;
fn compute244(x: int64, y: int64) -> int64;
fn compute245(x: int64, y: int64) -> int64;
fn compute246(x: int64, y: int64) -> int64;
fn compute247(x: int64, y: int64) -> int64;
fn compute248(x: int64, y: int64) -> int64;
fn compute249(x: int64, y: int64) -> int64;
fn compute250(x: int64, y: int64) -> int64;
fn compute251(x: int64, y: int64) -> int64;
fn compute252(x: int64, y: int64) -> int64;
fn compute253(x: int64, y: int64) -> int64;
fn compute254(x: int64, y: int64) -> int64;
fn compute255(x: int64, y: int64) -> int64;
fn compute256(x: int64, y: int64) -> int64;
fn compute257(x: int64, y: int64) -> int64;
fn compute258(x: int64, y: int64) -> int64;
fn compute259(x: int64, y: int64) -> int64;
fn compute260(x: int64, y: int64) -> int64;
fn compute261(x: int64, y: int64) -> int64;
fn compute262(x: int64, y: int64) -> int64;
fn compute263(x: int64, y: int64) -> int64;
fn compute264(x: int64, y: int64) -> int64;
fn compute265(x: int64, y: int64) -> int64;
fn compute266(x: int64, y: int64) -> int64;
fn compute267(x: int64, y: int64) -> int64;
fn compute268(x: int64, y: int64) -> int64;
fn compute269(x: int64, y: int64) -> int64;
fn compute270(x: int64, y: int64) -> int64;
fn compute271(x: int64, y: int64) -> int64;
fn compute272(x: int64, y: int64) -> int64;
fn compute273(x: int64, y: int64) -> int64;
fn compute274(x: int64, y: int64) -> int64;
fn compute275(x: int64, y: int64) -> int64;
fn compute276(x: int64, y: int64) -> int64;
fn compute277(x: int64, y: int64) -> int64;
fn compute278(x: int64, y: int64) -> int64;
fn compute279(x: int64, y: int64) -> int64;
fn compute280(x: int64, y: int64) -> int64;
fn compute281(x: int64, y: int64) -> int64;
fn compute282(x: int64, y: int64) -> int64;
fn compute283(x: int64, y: int64) -> int64;
fn compute284(x: int64, y: int64) -> int64;
fn compute285(x: int64, y: int64) -> int64;
fn compute286(x: int64, y: int64) -> int64;
fn compute287(x: int64, y: int64) -> int64;
fn compute288(x: int64, y: int64) -> int64;
fn compute289(x: int64, y: int64) -> int64;
fn compute290(x: int64, y: int64) -> int64;
fn compute291(x: int64, y: int64) -> int64;
fn compute292(x: int64, y: int64) -> int64;
fn compute293(x: int64, y: int64) -> int64;
fn compute294(x: int64, y: int64) -> int64;
fn compute295(x: int64, y: int64) -> int64;
fn compute296(x: int64, y: int64) -> int64;
fn compute297(x: int64, y: int64) -> int64;
fn compute298(x: int64, y: int64) -> int64;
fn compute299(x: int64, y: int64) -> int64;
fn compute300(x: int64, y: int64) -> int64;
fn compute301(x: int64, y: int64) -> int64;
fn compute302(x: int64, y: int64) -> int64;
fn compute303(x: int64, y: int64) -> int64;
fn compute304(x: int64, y: int64) -> int64;
fn compute305(x: int64, y: int64) -> int64;
fn compute306(x: int64, y: int64) -> int64;
fn compute307(x: int64, y: int64) -> int64;
fn compute308(x: int64, y: int64) -> int64;
fn compute309(x: int64, y: int64) -> int64;
fn compute310(x: int64, y: int64) -> int64;
fn compute311(x: int64, y: int64) -> int64;
fn compute312(x: int64, y: int64) -> int64;
fn compute313(x: int64, y: int64) -> int64;
fn compute314(x: int64, y: int64) -> int64;
fn compute315(x: int64, y: int64) -> int64;
fn compute316(x: int64, y: int64) -> int64;
fn compute317(x: int64, y: int64) -> int64;
fn compute318(x: int64, y: int64) -> int64;
fn compute319(x: int64, y: int64) -> int64;
fn compute320(x: int64, y: int64) -> int64;
fn compute321(x: int64, y: int64) -> int64;
fn compute322(x: int64, y: int64) -> int64;
fn compute323(x: int64, y: int64) -> int64;
fn compute324(x: int64, y: int64) -> int64;
fn compute325(x: int64, y: int64) -> int64;
fn compute326(x: int64, y: int64) -> int64;
fn compute327(x: int64, y: int64) -> int64;
fn compute328(x: int64, y: int64) -> int64;
fn compute329(x: int64, y: int64) -> int64;
fn compute330(x: int64, y: int64) -> int64;
fn compute331(x: int64, y: int64) -> int64;
fn compute332(x: int64, y: int64) -> int64;
fn compute333(x: int64, y: int64) -> int64;
fn compute334(x: int64, y: int64) -> int64;
fn compute335(x: int64, y: int64) -> int64;
fn compute336(x: int64, y: int64) -> int64;
fn compute337(x: int64, y: int64) -> int64;
fn compute338(x: int64, y: int64) -> int64;
fn compute339(x: int64, y: int64) -> int64;
fn compute340(x: int64, y: int64) -> int64;
fn compute341(x: int64, y: int64) -> int64;
fn compute342(x: int64, y: int64) -> int64;
fn compute343(x: int64, y: int64) -> int64;
fn compute344(x: int64, y: int64) -> int64;
fn compute345(x: int64, y: int64) -> int64;
fn compute346(x: int64, y: int64) -> int64;
fn compute347(x: int64, y: int64) -> int64;
fn compute348(x: int64, y: int64) -> int64;
fn compute349(x: int64, y: int64) -> int64;
fn compute350(x: int64, y: int64) -> int64;
fn compute351(x: int64, y: int64) -> int64;
fn compute352(x: int64, y: int64) -> int64;
fn compute353(x: int64, y: int64) -> int64;
fn compute354(x: int64, y: int64) -> int64;
fn compute355(x: int64, y: int64) -> int64;
fn compute356(x: int64, y: int64) -> int64;
fn compute357(x: int64, y: int64) -> int64;
fn compute358(x: int64, y: int64) -> int64;
fn compute359(x: int64, y: int64) -> int64;
fn compute360(x: int64, y: int64) -> int64;
fn compute361(x: int64, y: int64) -> int64;
fn compute362(x: int64, y: int64) -> int64;
fn compute363(x: int64, y: int64) -> int64;
fn compute364(x: int64, y: int64) -> int64;
fn compute365(x: int64, y: int64) -> int64;
fn compute366(x: int64, y: int64) -> int64;
fn compute367(x: int64, y: int64) -> int64;
fn compute368(x: int64, y: int64) -> int64;
fn compute369(x: int64, y: int64) -> int64;
fn compute370(x: int64, y: int64) -> int64;
fn compute371(x: int64, y: int64) -> int64;
fn compute372(x: int64, y: int64) -> int64;
fn compute373(x: int64, y: int64) -> int64;
fn compute374(x: int64, y: int64) -> int64;
fn compute375(x: int64, y: int64) -> int64;
fn compute376(x: int64, y: int64) -> int64;
fn compute377(x: int64, y: int64) -> int64;
fn compute378(x: int64, y: int64) -> int64;
fn compute379(x: int64, y: int64) -> int64;
fn compute380(x: int64, y: int64) -> int64;
fn compute381(x: int64, y: int64) -> int64;
fn compute382(x: int64, y: int64) -> int64;
fn compute383(x: int64, y: int64) -> int64;
fn compute384(x: int64, y: int64) -> int64;
fn compute385(x: int64, y: int64) -> int64;
fn compute386(x: int64, y: int64) -> int64;
fn compute387(x: int64, y: int64) -> int64;
fn compute388(x: int64, y: int64) -> int64;
fn compute389(x: int64, y: int64) -> int64;
fn compute390(x: int64, y: int64) -> int64;
fn compute391(x: int64, y: int64) -> int64;
fn compute392(x: int64, y: int64) -> int64;
fn compute393(x: int64, y: int64) -> int64;
fn compute394(x: int64, y: int64) -> int64;
fn compute395(x: int64, y: int64) -> int64;
fn compute396(x: int64, y: int64) -> int64;
fn compute397(x: int64, y: int64) -> int64;
fn compute398(x: int64, y: int64) -> int64;
fn compute399(x: int64, y: int64) -> int64;
fn compute400(x: int64, y: int64) -> int64;
fn compute401(x: int64, y: int64) -> int64;
fn compute402(x: int64, y: int64) -> int64;
fn compute403(x: int64, y: int64) -> int64;
fn compute404(x: int64, y: int64) -> int64;
fn compute405(x: int64, y: int64) -> int64;
fn compute406(x: int64, y: int64) -> int64;
fn compute407(x: int64, y: int64) -> int64;
fn compute408(x: int64, y: int64) -> int64;
fn compute409(x: int64, y: int64) -> int64;
fn compute410(x: int64, y: int64) -> int64;
fn compute411(x: int64, y: int64) -> int64;
fn compute412(x: int64, y: int64) -> int64;
fn compute413(x: int64, y: int64) -> int64;
fn compute414(x: int64, y: int64) -> int64;
fn compute415(x: int64, y: int64) -> int64;
fn compute416(x: int64, y: int64) -> int64;
fn compute417(x: int64, y: int64) -> int64;
fn compute418(x: int64, y: int64) -> int64;
fn compute419(x: int64, y: int64) -> int64;
fn compute420(x: int64, y: int64) -> int64;
fn compute421(x: int64, y: int64) -> int64;
fn compute422(x: int64, y: int64) -> int64;
fn compute423(x: int64, y: int64) -> int64;
fn compute424(x: int64, y: int64) -> int64;
fn compute425(x: int64, y: int64) -> int64;
fn compute426(x: int64, y: int64) -> int64;
fn compute427(x: int64, y: int64) -> int64;
fn compute428(x: int64, y: int64) -> int64;
fn compute429(x: int64, y: int64) -> int64;
fn compute430(x: int64, y: int64) -> int64;
fn compute431(x: int64, y: int64) -> int64;
fn compute432(x: int64, y: int64) -> int64;
fn compute433(x: int64, y: int64) -> int64;
fn compute434(x: int64, y: int64) -> int64;
fn compute435(x: int64, y: int64) -> int64;
fn compute436(x: int64, y: int64) -> int64;
fn compute437(x: int64, y: int64) -> int64;
fn compute438(x: int64, y: int64) -> int64;
fn compute439(x: int64, y: int64) -> int64;
fn compute440(x: int64, y: int64) -> int64;
fn compute441(x: int64, y: int64) -> int64;
fn compute442(x: int64, y: int64) -> int64;
fn compute443(x: int64, y: int64) -> int64;
fn compute444(x: int64, y: int64) -> int64;
fn compute445(x: int64, y: int64) -> int64;
fn compute446(x: int64, y: int64) -> int64;
fn compute447(x: int64, y: int64) -> int64;
fn compute448(x: int64, y: int64) -> int64;
fn compute449(x: int64, y: int64) -> int64;
fn compute450(x: int64, y: int64) -> int64;
fn compute451(x: int64, y: int64) -> int64;
fn compute452(x: int64, y: int64) -> int64;
fn compute453(x: int64, y: int64) -> int64;
fn compute454(x: int64, y: int64) -> int64;
fn compute455(x: int64, y: int64) -> int64;
fn compute456(x: int64, y: int64) -> int64;
fn compute457(x: int64, y: int64) -> int64;
fn compute458(x: int64, y: int64) -> int64;
fn compute459(x: int64, y: int64) -> int64;
fn compute460(x: int64, y: int64) -> int64;
fn compute461(x: int64, y: int64) -> int64;
fn compute462(x: int64, y: int64) -> int64;
fn compute463(x: int64, y: int64) -> int64;
fn compute464(x: int64, y: int64) -> int64;
fn compute465(x: int64, y: int64) -> int64;
fn compute466(x: int64, y: int64) -> int64;
fn compute467(x: int64, y: int64) -> int64;
fn compute468(x: int64, y: int64) -> int64;
fn compute469(x: int64, y: int64) -> int64;
fn compute470(x: int64, y: int64) -> int64;
fn compute471(x: int64, y: int64) -> int64;
fn compute472(x: int64, y: int64) -> int64;
fn compute473(x: int64, y: int64) -> int64;
fn compute474(x: int64, y: int64) -> int64;
fn compute475(x: int64, y: int64) -> int64;
fn compute476(x: int64, y: int64) -> int64;
fn compute477(x: int64, y: int64) -> int64;
fn compute478(x: int64, y: int64) -> int64;
fn compute479(x: int64, y: int64) -> int64;
fn compute480(x: int64, y: int64) -> int64;
fn compute481(x: int64, y: int64) -> int64;
fn compute482(x: int64, y: int64) -> int64;
fn compute483(x: int64, y: int64) -> int64;
fn compute484(x: int64, y: int64) -> int64;
fn compute485(x: int64, y: int64) -> int64;
fn compute486(x: int64, y: int64) -> int64;
fn compute487(x: int64, y: int64) -> int64;
fn compute488(x: int64, y: int64) -> int64;
fn compute489(x: int64, y: int64) -> int64;
fn compute490(x: int64, y: int64) -> int64;
fn compute491(x: int64, y: int64) -> int64;
fn compute492(x: int64, y: int64) -> int64;
fn compute493(x: int64, y: int64) -> int64;
fn compute494(x: int64, y: int64) -> int64;
fn compute495(x: int64, y: int64) -> int64;
fn compute496(x: int64, y: int64) -> int64;
fn compute497(x: int64, y: int64) -> int64;
fn compute498(x: int64, y: int64) -> int64;
fn compute499(x: int64, y: int64) -> int64;
fn compute500(x: int64, y: int64) -> int64;
fn compute501(x: int64, y: int64) -> int64;
fn compute502(x: int64, y: int64) -> int64;
fn compute503(x: int64, y: int64) -> int64;
fn compute504(x: int64, y: int64) -> int64;
fn compute505(x: int64, y: int64) -> int64;
fn compute506(x: int64, y: int64) -> int64;
fn compute507(x: int64, y: int64) -> int64;
fn compute508(x: int64, y: int64) -> int64;
fn compute509(x: int64, y: int64) -> int64;
fn compute510(x: int64, y: int64) -> int64;
fn compute511(x: int64, y: int64) -> int64;
fn compute512(x: int64, y: int64) -> int64;
fn compute513(x: int64, y: int64) -> int64;
fn compute514(x: int64, y: int64) -> int64;
fn compute515(x: int64, y: int64) -> int64;
fn compute516(x: int64, y: int64) -> int64;
fn compute517(x: int64, y: int64) -> int64;
fn compute518(x: int64, y: int64) -> int64;
fn compute519(x: int64, y: int64) -> int64;

fn compute0(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + (k * v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute1(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + (k * v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute2(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute1(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute3(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute1(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute4(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute2(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute5(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute2(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute6(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute3(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute7(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute3(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute8(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute4(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute9(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute4(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute10(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute5(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute11(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute5(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute12(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute6(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute13(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute6(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute14(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute7(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute15(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute7(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute16(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute8(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute17(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute8(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute18(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute9(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute19(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute9(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute20(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute10(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute21(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute10(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute22(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute11(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute23(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute11(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute24(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute12(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute25(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute12(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute26(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute13(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute27(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute13(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute28(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute14(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute29(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute14(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute30(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute15(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute31(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute15(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute32(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute16(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute33(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute16(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute34(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute17(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute35(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute17(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute36(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute18(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute37(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute18(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute38(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute19(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute39(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute19(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute40(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute20(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute41(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute20(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute42(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute21(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute43(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute21(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute44(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute22(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute45(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute22(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute46(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute23(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute47(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute23(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute48(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute24(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute49(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute24(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute50(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute25(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute51(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute25(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute52(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute26(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute53(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute26(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute54(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute27(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute55(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute27(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute56(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute28(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute57(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute28(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute58(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute29(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute59(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute29(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute60(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute30(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute61(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute30(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute62(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute31(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute63(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute31(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute64(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute32(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute65(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute32(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute66(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute33(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute67(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute33(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute68(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute34(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute69(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute34(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute70(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute35(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute71(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute35(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute72(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute36(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute73(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute36(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute74(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute37(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute75(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute37(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute76(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute38(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute77(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute38(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute78(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute39(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute79(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute39(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute80(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute40(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute81(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute40(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute82(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute41(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute83(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute41(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute84(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute42(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute85(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute42(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute86(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute43(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute87(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute43(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute88(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute44(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute89(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute44(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute90(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute45(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute91(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute45(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute92(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute46(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute93(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute46(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute94(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute47(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute95(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute47(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute96(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute48(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute97(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute48(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute98(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute49(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute99(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute49(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute100(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute50(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute101(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute50(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute102(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute51(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute103(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute51(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute104(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute52(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute105(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute52(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute106(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute53(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute107(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute53(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute108(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute54(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute109(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute54(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute110(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute55(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute111(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute55(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute112(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute56(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute113(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute56(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute114(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute57(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute115(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute57(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute116(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute58(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute117(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute58(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute118(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute59(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute119(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute59(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute120(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute60(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute121(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute60(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute122(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute61(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute123(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute61(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute124(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute62(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute125(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute62(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute126(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute63(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute127(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute63(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute128(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute64(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute129(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute64(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute130(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute65(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute131(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute65(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute132(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute66(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute133(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute66(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute134(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute67(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute135(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute67(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute136(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute68(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute137(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute68(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute138(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute69(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute139(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute69(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute140(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute70(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute141(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute70(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute142(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute71(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute143(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute71(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute144(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute72(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute145(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute72(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute146(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute73(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute147(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute73(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute148(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute74(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute149(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute74(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute150(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute75(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute151(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute75(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute152(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute76(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute153(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute76(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute154(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute77(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute155(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute77(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute156(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute78(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute157(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute78(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute158(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute79(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute159(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute79(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute160(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute80(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute161(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute80(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute162(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute81(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute163(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute81(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute164(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute82(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute165(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute82(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute166(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute83(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute167(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute83(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute168(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute84(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute169(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute84(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute170(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute85(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute171(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute85(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute172(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute86(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute173(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute86(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute174(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute87(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute175(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute87(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute176(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute88(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute177(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute88(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute178(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute89(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute179(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute89(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute180(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute90(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute181(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute90(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute182(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute91(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute183(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute91(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute184(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute92(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute185(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute92(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute186(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute93(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute187(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute93(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute188(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute94(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute189(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute94(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute190(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute95(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute191(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute95(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute192(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute96(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute193(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute96(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute194(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute97(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute195(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute97(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute196(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute98(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute197(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute98(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute198(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute99(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute199(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute99(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute200(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute100(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute201(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute100(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute202(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute101(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute203(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute101(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute204(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute102(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute205(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute102(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute206(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute103(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute207(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute103(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute208(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute104(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute209(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute104(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute210(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute105(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute211(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute105(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute212(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute106(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute213(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute106(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute214(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute107(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute215(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute107(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute216(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute108(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute217(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute108(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute218(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute109(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute219(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute109(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute220(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute110(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute221(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute110(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute222(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute111(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute223(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute111(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute224(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute112(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute225(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute112(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute226(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute113(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute227(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute113(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute228(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute114(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute229(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute114(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute230(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute115(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute231(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute115(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute232(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute116(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute233(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute116(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute234(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute117(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute235(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute117(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute236(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute118(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute237(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute118(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute238(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute119(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute239(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute119(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute240(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute120(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute241(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute120(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute242(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute121(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute243(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute121(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute244(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute122(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute245(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute122(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute246(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute123(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute247(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute123(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute248(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute124(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute249(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute124(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute250(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute125(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute251(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute125(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute252(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute126(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute253(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute126(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute254(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute127(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute255(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute127(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute256(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute128(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute257(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute128(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute258(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute129(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute259(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute129(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute260(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute130(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute261(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute130(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute262(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute131(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute263(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute131(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute264(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute132(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute265(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute132(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute266(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute133(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute267(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute133(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute268(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute134(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute269(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute134(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute270(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute135(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute271(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute135(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute272(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute136(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute273(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute136(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute274(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute137(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute275(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute137(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute276(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute138(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute277(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute138(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute278(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute139(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute279(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute139(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute280(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute140(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute281(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute140(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute282(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute141(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute283(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute141(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute284(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute142(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute285(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute142(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute286(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute143(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute287(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute143(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute288(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute144(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute289(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute144(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute290(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute145(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute291(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute145(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute292(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute146(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute293(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute146(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute294(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute147(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute295(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute147(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute296(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute148(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute297(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute148(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute298(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute149(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute299(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute149(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute300(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute150(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute301(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute150(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute302(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute151(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute303(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute151(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute304(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute152(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute305(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute152(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute306(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute153(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute307(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute153(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute308(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute154(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute309(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute154(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute310(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute155(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute311(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute155(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute312(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute156(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute313(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute156(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute314(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute157(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute315(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute157(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute316(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute158(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute317(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute158(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute318(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute159(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute319(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute159(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute320(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute160(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute321(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute160(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute322(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute161(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute323(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute161(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute324(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute162(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute325(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute162(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute326(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute163(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute327(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute163(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute328(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute164(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute329(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute164(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute330(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute165(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute331(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute165(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute332(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute166(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute333(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute166(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute334(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute167(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute335(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute167(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute336(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute168(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute337(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute168(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute338(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute169(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute339(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute169(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute340(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute170(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute341(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute170(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute342(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute171(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute343(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute171(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute344(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute172(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute345(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute172(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute346(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute173(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute347(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute173(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute348(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute174(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute349(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute174(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute350(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute175(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute351(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute175(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute352(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute176(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute353(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute176(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute354(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute177(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute355(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute177(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute356(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute178(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute357(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute178(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute358(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute179(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute359(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute179(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute360(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute180(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute361(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute180(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute362(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute181(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute363(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute181(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute364(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute182(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute365(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute182(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute366(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute183(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute367(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute183(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute368(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute184(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute369(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute184(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute370(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute185(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute371(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute185(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute372(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute186(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute373(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute186(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute374(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute187(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute375(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute187(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute376(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute188(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute377(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute188(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute378(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute189(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute379(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute189(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute380(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute190(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute381(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute190(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute382(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute191(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute383(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute191(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute384(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute192(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute385(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute192(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute386(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute193(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute387(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute193(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute388(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute194(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute389(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute194(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute390(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute195(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute391(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute195(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute392(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute196(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute393(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute196(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute394(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute197(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute395(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute197(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute396(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute198(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute397(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute198(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute398(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute199(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute399(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute199(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute400(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute200(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute401(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute200(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute402(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute201(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute403(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute201(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute404(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute202(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute405(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute202(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute406(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute203(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute407(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute203(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute408(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute204(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute409(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute204(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute410(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute205(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute411(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute205(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute412(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute206(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute413(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute206(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute414(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute207(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute415(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute207(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute416(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute208(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute417(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute208(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute418(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute209(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute419(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute209(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute420(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute210(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute421(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute210(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute422(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute211(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute423(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute211(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute424(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute212(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute425(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute212(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute426(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute213(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute427(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute213(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute428(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute214(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute429(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute214(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute430(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute215(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute431(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute215(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute432(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute216(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute433(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute216(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute434(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute217(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute435(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute217(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute436(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute218(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute437(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute218(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute438(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute219(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute439(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute219(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute440(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute220(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute441(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute220(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute442(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute221(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute443(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute221(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute444(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute222(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute445(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute222(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute446(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute223(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute447(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute223(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute448(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute224(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute449(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute224(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute450(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute225(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute451(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute225(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute452(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute226(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute453(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute226(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute454(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute227(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute455(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute227(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute456(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute228(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute457(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute228(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute458(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute229(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute459(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute229(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute460(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute230(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute461(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute230(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute462(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute231(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute463(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute231(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute464(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute232(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute465(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute232(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute466(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute233(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute467(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute233(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute468(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute234(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute469(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute234(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute470(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute235(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute471(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute235(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute472(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute236(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute473(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute236(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute474(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute237(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute475(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute237(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute476(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute238(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute477(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute238(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute478(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute239(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute479(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute239(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute480(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute240(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute481(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute240(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute482(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute241(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute483(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute241(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute484(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute242(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute485(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute242(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute486(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute243(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute487(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute243(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute488(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute244(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute489(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute244(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute490(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute245(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute491(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute245(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute492(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute246(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute493(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute246(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute494(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute247(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute495(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute247(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute496(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute248(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute497(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute248(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute498(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute249(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute499(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute249(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute500(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute250(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute501(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute250(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute502(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute251(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute503(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute251(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute504(x: int64, y: int64) -> int64 {
  var v: S8;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute252(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute505(x: int64, y: int64) -> int64 {
  var v: S9;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute252(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute506(x: int64, y: int64) -> int64 {
  var v: S10;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute253(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute507(x: int64, y: int64) -> int64 {
  var v: S11;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute253(k, v.a) % (int64)2;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute508(x: int64, y: int64) -> int64 {
  var v: S12;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute254(k, v.a) % (int64)3;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute509(x: int64, y: int64) -> int64 {
  var v: S13;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute254(k, v.a) % (int64)4;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute510(x: int64, y: int64) -> int64 {
  var v: S14;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute255(k, v.a) % (int64)5;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute511(x: int64, y: int64) -> int64 {
  var v: S15;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute255(k, v.a) % (int64)6;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute512(x: int64, y: int64) -> int64 {
  var v: S0;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute256(k, v.a) % (int64)7;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute513(x: int64, y: int64) -> int64 {
  var v: S1;
  v.a = x + y;
  v.b = x * (int64)3;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute256(k, v.a) % (int64)8;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute514(x: int64, y: int64) -> int64 {
  var v: S2;
  v.a = x + y;
  v.b = x * (int64)4;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute257(k, v.a) % (int64)9;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute515(x: int64, y: int64) -> int64 {
  var v: S3;
  v.a = x + y;
  v.b = x * (int64)5;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)3; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute257(k, v.a) % (int64)10;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute516(x: int64, y: int64) -> int64 {
  var v: S4;
  v.a = x + y;
  v.b = x * (int64)6;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)4; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute258(k, v.a) % (int64)11;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute517(x: int64, y: int64) -> int64 {
  var v: S5;
  v.a = x + y;
  v.b = x * (int64)7;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)5; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute258(k, v.a) % (int64)12;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute518(x: int64, y: int64) -> int64 {
  var v: S6;
  v.a = x + y;
  v.b = x * (int64)1;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)6; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute259(k, v.a) % (int64)13;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn compute519(x: int64, y: int64) -> int64 {
  var v: S7;
  v.a = x + y;
  v.b = x * (int64)2;
  v.c = (int64)0;
  var acc: int64 = (int64)0;
  for (var k: int64 = (int64)0; k < (int64)7; k = k + (int64)1) {
    if (k > v.a) {
      acc = acc + v.b - k;
    } else {
      acc = acc + compute259(k, v.a) % (int64)14;
    }
  }
  return acc + v.a + v.b + v.c;
}

fn main() -> int32 {
  var total: int64 = (int64)0;
  total = total + compute0((int64)0, (int64)0);
  total = total + compute1((int64)1, (int64)1);
  total = total + compute2((int64)2, (int64)2);
  total = total + compute3((int64)3, (int64)3);
  total = total + compute4((int64)4, (int64)4);
  total = total + compute5((int64)5, (int64)5);
  total = total + compute6((int64)6, (int64)6);
  total = total + compute7((int64)7, (int64)7);
  total = total + compute8((int64)8, (int64)8);
  total = total + compute9((int64)9, (int64)9);
  total = total + compute10((int64)10, (int64)10);
  total = total + compute11((int64)11, (int64)11);
  total = total + compute12((int64)12, (int64)12);
  total = total + compute13((int64)13, (int64)13);
  total = total + compute14((int64)14, (int64)14);
  total = total + compute15((int64)15, (int64)15);
  total = total + compute16((int64)16, (int64)16);
  total = total + compute17((int64)17, (int64)17);
  total = total + compute18((int64)18, (int64)18);
  total = total + compute19((int64)19, (int64)19);
  total = total + compute20((int64)20, (int64)20);
  total = total + compute21((int64)21, (int64)21);
  total = total + compute22((int64)22, (int64)22);
  total = total + compute23((int64)23, (int64)23);
  total = total + compute24((int64)24, (int64)24);
  total = total + compute25((int64)25, (int64)25);
  total = total + compute26((int64)26, (int64)26);
  total = total + compute27((int64)27, (int64)27);
  total = total + compute28((int64)28, (int64)28);
  total = total + compute29((int64)29, (int64)29);
  total = total + compute30((int64)30, (int64)30);
  total = total + compute31((int64)31, (int64)31);
  total = total + compute32((int64)32, (int64)32);
  total = total + compute33((int64)33, (int64)33);
  total = total + compute34((int64)34, (int64)34);
  total = total + compute35((int64)35, (int64)35);
  total = total + compute36((int64)36, (int64)36);
  total = total + compute37((int64)37, (int64)37);
  total = total + compute38((int64)38, (int64)38);
  total = total + compute39((int64)39, (int64)39);
  total = total + compute40((int64)40, (int64)40);
  total = total + compute41((int64)41, (int64)41);
  total = total + compute42((int64)42, (int64)42);
  total = total + compute43((int64)43, (int64)43);
  total = total + compute44((int64)44, (int64)44);
  total = total + compute45((int64)45, (int64)45);
  total = total + compute46((int64)46, (int64)46);
  total = total + compute47((int64)47, (int64)47);
  total = total + compute48((int64)48, (int64)48);
  total = total + compute49((int64)49, (int64)49);
  total = total + compute50((int64)50, (int64)50);
  total = total + compute51((int64)51, (int64)51);
  total = total + compute52((int64)52, (int64)52);
  total = total + compute53((int64)53, (int64)0);
  total = total + compute54((int64)54, (int64)1);
  total = total + compute55((int64)55, (int64)2);
  total = total + compute56((int64)56, (int64)3);
  total = total + compute57((int64)57, (int64)4);
  total = total + compute58((int64)58, (int64)5);
  total = total + compute59((int64)59, (int64)6);
  total = total + compute60((int64)60, (int64)7);
  total = total + compute61((int64)61, (int64)8);
  total = total + compute62((int64)62, (int64)9);
  total = total + compute63((int64)63, (int64)10);
  total = total + compute64((int64)64, (int64)11);
  total = total + compute65((int64)65, (int64)12);
  total = total + compute66((int64)66, (int64)13);
  total = total + compute67((int64)67, (int64)14);
  total = total + compute68((int64)68, (int64)15);
  total = total + compute69((int64)69, (int64)16);
  total = total + compute70((int64)70, (int64)17);
  total = total + compute71((int64)71, (int64)18);
  total = total + compute72((int64)72, (int64)19);
  total = total + compute73((int64)73, (int64)20);
  total = total + compute74((int64)74, (int64)21);
  total = total + compute75((int64)75, (int64)22);
  total = total + compute76((int64)76, (int64)23);
  total = total + compute77((int64)77, (int64)24);
  total = total + compute78((int64)78, (int64)25);
  total = total + compute79((int64)79, (int64)26);
  total = total + compute80((int64)80, (int64)27);
  total = total + compute81((int64)81, (int64)28);
  total = total + compute82((int64)82, (int64)29);
  total = total + compute83((int64)83, (int64)30);
  total = total + compute84((int64)84, (int64)31);
  total = total + compute85((int64)85, (int64)32);
  total = total + compute86((int64)86, (int64)33);
  total = total + compute87((int64)87, (int64)34);
  total = total + compute88((int64)88, (int64)35);
  total = total + compute89((int64)89, (int64)36);
  total = total + compute90((int64)90, (int64)37);
  total = total + compute91((int64)91, (int64)38);
  total = total + compute92((int64)92, (int64)39);
  total = total + compute93((int64)93, (int64)40);
  total = total + compute94((int64)94, (int64)41);
  total = total + compute95((int64)95, (int64)42);
  total = total + compute96((int64)96, (int64)43);
  total = total + compute97((int64)0, (int64)44);
  total = total + compute98((int64)1, (int64)45);
  total = total + compute99((int64)2, (int64)46);
  total = total + compute100((int64)3, (int64)47);
  total = total + compute101((int64)4, (int64)48);
  total = total + compute102((int64)5, (int64)49);
  total = total + compute103((int64)6, (int64)50);
  total = total + compute104((int64)7, (int64)51);
  total = total + compute105((int64)8, (int64)52);
  total = total + compute106((int64)9, (int64)0);
  total = total + compute107((int64)10, (int64)1);
  total = total + compute108((int64)11, (int64)2);
  total = total + compute109((int64)12, (int64)3);
  total = total + compute110((int64)13, (int64)4);
  total = total + compute111((int64)14, (int64)5);
  total = total + compute112((int64)15, (int64)6);
  total = total + compute113((int64)16, (int64)7);
  total = total + compute114((int64)17, (int64)8);
  total = total + compute115((int64)18, (int64)9);
  total = total + compute116((int64)19, (int64)10);
  total = total + compute117((int64)20, (int64)11);
  total = total + compute118((int64)21, (int64)12);
  total = total + compute119((int64)22, (int64)13);
  total = total + compute120((int64)23, (int64)14);
  total = total + compute121((int64)24, (int64)15);
  total = total + compute122((int64)25, (int64)16);
  total = total + compute123((int64)26, (int64)17);
  total = total + compute124((int64)27, (int64)18);
  total = total + compute125((int64)28, (int64)19);
  total = total + compute126((int64)29, (int64)20);
  total = total + compute127((int64)30, (int64)21);
  total = total + compute128((int64)31, (int64)22);
  total = total + compute129((int64)32, (int64)23);
  total = total + compute130((int64)33, (int64)24);
  total = total + compute131((int64)34, (int64)25);
  total = total + compute132((int64)35, (int64)26);
  total = total + compute133((int64)36, (int64)27);
  total = total + compute134((int64)37, (int64)28);
  total = total + compute135((int64)38, (int64)29);
  total = total + compute136((int64)39, (int64)30);
  total = total + compute137((int64)40, (int64)31);
  total = total + compute138((int64)41, (int64)32);
  total = total + compute139((int64)42, (int64)33);
  total = total + compute140((int64)43, (int64)34);
  total = total + compute141((int64)44, (int64)35);
  total = total + compute142((int64)45, (int64)36);
  total = total + compute143((int64)46, (int64)37);
  total = total + compute144((int64)47, (int64)38);
  total = total + compute145((int64)48, (int64)39);
  total = total + compute146((int64)49, (int64)40);
  total = total + compute147((int64)50, (int64)41);
  total = total + compute148((int64)51, (int64)42);
  total = total + compute149((int64)52, (int64)43);
  total = total + compute150((int64)53, (int64)44);
  total = total + compute151((int64)54, (int64)45);
  total = total + compute152((int64)55, (int64)46);
  total = total + compute153((int64)56, (int64)47);
  total = total + compute154((int64)57, (int64)48);
  total = total + compute155((int64)58, (int64)49);
  total = total + compute156((int64)59, (int64)50);
  total = total + compute157((int64)60, (int64)51);
  total = total + compute158((int64)61, (int64)52);
  total = total + compute159((int64)62, (int64)0);
  total = total + compute160((int64)63, (int64)1);
  total = total + compute161((int64)64, (int64)2);
  total = total + compute162((int64)65, (int64)3);
  total = total + compute163((int64)66, (int64)4);
  total = total + compute164((int64)67, (int64)5);
  total = total + compute165((int64)68, (int64)6);
  total = total + compute166((int64)69, (int64)7);
  total = total + compute167((int64)70, (int64)8);
  total = total + compute168((int64)71, (int64)9);
  total = total + compute169((int64)72, (int64)10);
  total = total + compute170((int64)73, (int64)11);
  total = total + compute171((int64)74, (int64)12);
  total = total + compute172((int64)75, (int64)13);
  total = total + compute173((int64)76, (int64)14);
  total = total + compute174((int64)77, (int64)15);
  total = total + compute175((int64)78, (int64)16);
  total = total + compute176((int64)79, (int64)17);
  total = total + compute177((int64)80, (int64)18);
  total = total + compute178((int64)81, (int64)19);
  total = total + compute179((int64)82, (int64)20);
  total = total + compute180((int64)83, (int64)21);
  total = total + compute181((int64)84, (int64)22);
  total = total + compute182((int64)85, (int64)23);
  total = total + compute183((int64)86, (int64)24);
  total = total + compute184((int64)87, (int64)25);
  total = total + compute185((int64)88, (int64)26);
  total = total + compute186((int64)89, (int64)27);
  total = total + compute187((int64)90, (int64)28);
  total = total + compute188((int64)91, (int64)29);
  total = total + compute189((int64)92, (int64)30);
  total = total + compute190((int64)93, (int64)31);
  total = total + compute191((int64)94, (int64)32);
  total = total + compute192((int64)95, (int64)33);
  total = total + compute193((int64)96, (int64)34);
  total = total + compute194((int64)0, (int64)35);
  total = total + compute195((int64)1, (int64)36);
  total = total + compute196((int64)2, (int64)37);
  total = total + compute197((int64)3, (int64)38);
  total = total + compute198((int64)4, (int64)39);
  total = total + compute199((int64)5, (int64)40);
  total = total + compute200((int64)6, (int64)41);
  total = total + compute201((int64)7, (int64)42);
  total = total + compute202((int64)8, (int64)43);
  total = total + compute203((int64)9, (int64)44);
  total = total + compute204((int64)10, (int64)45);
  total = total + compute205((int64)11, (int64)46);
  total = total + compute206((int64)12, (int64)47);
  total = total + compute207((int64)13, (int64)48);
  total = total + compute208((int64)14, (int64)49);
  total = total + compute209((int64)15, (int64)50);
  total = total + compute210((int64)16, (int64)51);
  total = total + compute211((int64)17, (int64)52);
  total = total + compute212((int64)18, (int64)0);
  total = total + compute213((int64)19, (int64)1);
  total = total + compute214((int64)20, (int64)2);
  total = total + compute215((int64)21, (int64)3);
  total = total + compute216((int64)22, (int64)4);
  total = total + compute217((int64)23, (int64)5);
  total = total + compute218((int64)24, (int64)6);
  total = total + compute219((int64)25, (int64)7);
  total = total + compute220((int64)26, (int64)8);
  total = total + compute221((int64)27, (int64)9);
  total = total + compute222((int64)28, (int64)10);
  total = total + compute223((int64)29, (int64)11);
  total = total + compute224((int64)30, (int64)12);
  total = total + compute225((int64)31, (int64)13);
  total = total + compute226((int64)32, (int64)14);
  total = total + compute227((int64)33, (int64)15);
  total = total + compute228((int64)34, (int64)16);
  total = total + compute229((int64)35, (int64)17);
  total = total + compute230((int64)36, (int64)18);
  total = total + compute231((int64)37, (int64)19);
  total = total + compute232((int64)38, (int64)20);
  total = total + compute233((int64)39, (int64)21);
  total = total + compute234((int64)40, (int64)22);
  total = total + compute235((int64)41, (int64)23);
  total = total + compute236((int64)42, (int64)24);
  total = total + compute237((int64)43, (int64)25);
  total = total + compute238((int64)44, (int64)26);
  total = total + compute239((int64)45, (int64)27);
  total = total + compute240((int64)46, (int64)28);
  total = total + compute241((int64)47, (int64)29);
  total = total + compute242((int64)48, (int64)30);
  total = total + compute243((int64)49, (int64)31);
  total = total + compute244((int64)50, (int64)32);
  total = total + compute245((int64)51, (int64)33);
  total = total + compute246((int64)52, (int64)34);
  total = total + compute247((int64)53, (int64)35);
  total = total + compute248((int64)54, (int64)36);
  total = total + compute249((int64)55, (int64)37);
  total = total + compute250((int64)56, (int64)38);
  total = total + compute251((int64)57, (int64)39);
  total = total + compute252((int64)58, (int64)40);
  total = total + compute253((int64)59, (int64)41);
  total = total + compute254((int64)60, (int64)42);
  total = total + compute255((int64)61, (int64)43);
  total = total + compute256((int64)62, (int64)44);
  total = total + compute257((int64)63, (int64)45);
  total = total + compute258((int64)64, (int64)46);
  total = total + compute259((int64)65, (int64)47);
  total = total + compute260((int64)66, (int64)48);
  total = total + compute261((int64)67, (int64)49);
  total = total + compute262((int64)68, (int64)50);
  total = total + compute263((int64)69, (int64)51);
  total = total + compute264((int64)70, (int64)52);
  total = total + compute265((int64)71, (int64)0);
  total = total + compute266((int64)72, (int64)1);
  total = total + compute267((int64)73, (int64)2);
  total = total + compute268((int64)74, (int64)3);
  total = total + compute269((int64)75, (int64)4);
  total = total + compute270((int64)76, (int64)5);
  total = total + compute271((int64)77, (int64)6);
  total = total + compute272((int64)78, (int64)7);
  total = total + compute273((int64)79, (int64)8);
  total = total + compute274((int64)80, (int64)9);
  total = total + compute275((int64)81, (int64)10);
  total = total + compute276((int64)82, (int64)11);
  total = total + compute277((int64)83, (int64)12);
  total = total + compute278((int64)84, (int64)13);
  total = total + compute279((int64)85, (int64)14);
  total = total + compute280((int64)86, (int64)15);
  total = total + compute281((int64)87, (int64)16);
  total = total + compute282((int64)88, (int64)17);
  total = total + compute283((int64)89, (int64)18);
  total = total + compute284((int64)90, (int64)19);
  total = total + compute285((int64)91, (int64)20);
  total = total + compute286((int64)92, (int64)21);
  total = total + compute287((int64)93, (int64)22);
  total = total + compute288((int64)94, (int64)23);
  total = total + compute289((int64)95, (int64)24);
  total = total + compute290((int64)96, (int64)25);
  total = total + compute291((int64)0, (int64)26);
  total = total + compute292((int64)1, (int64)27);
  total = total + compute293((int64)2, (int64)28);
  total = total + compute294((int64)3, (int64)29);
  total = total + compute295((int64)4, (int64)30);
  total = total + compute296((int64)5, (int64)31);
  total = total + compute297((int64)6, (int64)32);
  total = total + compute298((int64)7, (int64)33);
  total = total + compute299((int64)8, (int64)34);
  total = total + compute300((int64)9, (int64)35);
  total = total + compute301((int64)10, (int64)36);
  total = total + compute302((int64)11, (int64)37);
  total = total + compute303((int64)12, (int64)38);
  total = total + compute304((int64)13, (int64)39);
  total = total + compute305((int64)14, (int64)40);
  total = total + compute306((int64)15, (int64)41);
  total = total + compute307((int64)16, (int64)42);
  total = total + compute308((int64)17, (int64)43);
  total = total + compute309((int64)18, (int64)44);
  total = total + compute310((int64)19, (int64)45);
  total = total + compute311((int64)20, (int64)46);
  total = total + compute312((int64)21, (int64)47);
  total = total + compute313((int64)22, (int64)48);
  total = total + compute314((int64)23, (int64)49);
  total = total + compute315((int64)24, (int64)50);
  total = total + compute316((int64)25, (int64)51);
  total = total + compute317((int64)26, (int64)52);
  total = total + compute318((int64)27, (int64)0);
  total = total + compute319((int64)28, (int64)1);
  total = total + compute320((int64)29, (int64)2);
  total = total + compute321((int64)30, (int64)3);
  total = total + compute322((int64)31, (int64)4);
  total = total + compute323((int64)32, (int64)5);
  total = total + compute324((int64)33, (int64)6);
  total = total + compute325((int64)34, (int64)7);
  total = total + compute326((int64)35, (int64)8);
  total = total + compute327((int64)36, (int64)9);
  total = total + compute328((int64)37, (int64)10);
  total = total + compute329((int64)38, (int64)11);
  total = total + compute330((int64)39, (int64)12);
  total = total + compute331((int64)40, (int64)13);
  total = total + compute332((int64)41, (int64)14);
  total = total + compute333((int64)42, (int64)15);
  total = total + compute334((int64)43, (int64)16);
  total = total + compute335((int64)44, (int64)17);
  total = total + compute336((int64)45, (int64)18);
  total = total + compute337((int64)46, (int64)19);
  total = total + compute338((int64)47, (int64)20);
  total = total + compute339((int64)48, (int64)21);
  total = total + compute340((int64)49, (int64)22);
  total = total + compute341((int64)50, (int64)23);
  total = total + compute342((int64)51, (int64)24);
  total = total + compute343((int64)52, (int64)25);
  total = total + compute344((int64)53, (int64)26);
  total = total + compute345((int64)54, (int64)27);
  total = total + compute346((int64)55, (int64)28);
  total = total + compute347((int64)56, (int64)29);
  total = total + compute348((int64)57, (int64)30);
  total = total + compute349((int64)58, (int64)31);
  total = total + compute350((int64)59, (int64)32);
  total = total + compute351((int64)60, (int64)33);
  total = total + compute352((int64)61, (int64)34);
  total = total + compute353((int64)62, (int64)35);
  total = total + compute354((int64)63, (int64)36);
  total = total + compute355((int64)64, (int64)37);
  total = total + compute356((int64)65, (int64)38);
  total = total + compute357((int64)66, (int64)39);
  total = total + compute358((int64)67, (int64)40);
  total = total + compute359((int64)68, (int64)41);
  total = total + compute360((int64)69, (int64)42);
  total = total + compute361((int64)70, (int64)43);
  total = total + compute362((int64)71, (int64)44);
  total = total + compute363((int64)72, (int64)45);
  total = total + compute364((int64)73, (int64)46);
  total = total + compute365((int64)74, (int64)47);
  total = total + compute366((int64)75, (int64)48);
  total = total + compute367((int64)76, (int64)49);
  total = total + compute368((int64)77, (int64)50);
  total = total + compute369((int64)78, (int64)51);
  total = total + compute370((int64)79, (int64)52);
  total = total + compute371((int64)80, (int64)0);
  total = total + compute372((int64)81, (int64)1);
  total = total + compute373((int64)82, (int64)2);
  total = total + compute374((int64)83, (int64)3);
  total = total + compute375((int64)84, (int64)4);
  total = total + compute376((int64)85, (int64)5);
  total = total + compute377((int64)86, (int64)6);
  total = total + compute378((int64)87, (int64)7);
  total = total + compute379((int64)88, (int64)8);
  total = total + compute380((int64)89, (int64)9);
  total = total + compute381((int64)90, (int64)10);
  total = total + compute382((int64)91, (int64)11);
  total = total + compute383((int64)92, (int64)12);
  total = total + compute384((int64)93, (int64)13);
  total = total + compute385((int64)94, (int64)14);
  total = total + compute386((int64)95, (int64)15);
  total = total + compute387((int64)96, (int64)16);
  total = total + compute388((int64)0, (int64)17);
  total = total + compute389((int64)1, (int64)18);
  total = total + compute390((int64)2, (int64)19);
  total = total + compute391((int64)3, (int64)20);
  total = total + compute392((int64)4, (int64)21);
  total = total + compute393((int64)5, (int64)22);
  total = total + compute394((int64)6, (int64)23);
  total = total + compute395((int64)7, (int64)24);
  total = total + compute396((int64)8, (int64)25);
  total = total + compute397((int64)9, (int64)26);
  total = total + compute398((int64)10, (int64)27);
  total = total + compute399((int64)11, (int64)28);
  total = total + compute400((int64)12, (int64)29);
  total = total + compute401((int64)13, (int64)30);
  total = total + compute402((int64)14, (int64)31);
  total = total + compute403((int64)15, (int64)32);
  total = total + compute404((int64)16, (int64)33);
  total = total + compute405((int64)17, (int64)34);
  total = total + compute406((int64)18, (int64)35);
  total = total + compute407((int64)19, (int64)36);
  total = total + compute408((int64)20, (int64)37);
  total = total + compute409((int64)21, (int64)38);
  total = total + compute410((int64)22, (int64)39);
  total = total + compute411((int64)23, (int64)40);
  total = total + compute412((int64)24, (int64)41);
  total = total + compute413((int64)25, (int64)42);
  total = total + compute414((int64)26, (int64)43);
  total = total + compute415((int64)27, (int64)44);
  total = total + compute416((int64)28, (int64)45);
  total = total + compute417((int64)29, (int64)46);
  total = total + compute418((int64)30, (int64)47);
  total = total + compute419((int64)31, (int64)48);
  total = total + compute420((int64)32, (int64)49);
  total = total + compute421((int64)33, (int64)50);
  total = total + compute422((int64)34, (int64)51);
  total = total + compute423((int64)35, (int64)52);
  total = total + compute424((int64)36, (int64)0);
  total = total + compute425((int64)37, (int64)1);
  total = total + compute426((int64)38, (int64)2);
  total = total + compute427((int64)39, (int64)3);
  total = total + compute428((int64)40, (int64)4);
  total = total + compute429((int64)41, (int64)5);
  total = total + compute430((int64)42, (int64)6);
  total = total + compute431((int64)43, (int64)7);
  total = total + compute432((int64)44, (int64)8);
  total = total + compute433((int64)45, (int64)9);
  total = total + compute434((int64)46, (int64)10);
  total = total + compute435((int64)47, (int64)11);
  total = total + compute436((int64)48, (int64)12);
  total = total + compute437((int64)49, (int64)13);
  total = total + compute438((int64)50, (int64)14);
  total = total + compute439((int64)51, (int64)15);
  total = total + compute440((int64)52, (int64)16);
  total = total + compute441((int64)53, (int64)17);
  total = total + compute442((int64)54, (int64)18);
  total = total + compute443((int64)55, (int64)19);
  total = total + compute444((int64)56, (int64)20);
  total = total + compute445((int64)57, (int64)21);
  total = total + compute446((int64)58, (int64)22);
  total = total + compute447((int64)59, (int64)23);
  total = total + compute448((int64)60, (int64)24);
  total = total + compute449((int64)61, (int64)25);
  total = total + compute450((int64)62, (int64)26);
  total = total + compute451((int64)63, (int64)27);
  total = total + compute452((int64)64, (int64)28);
  total = total + compute453((int64)65, (int64)29);
  total = total + compute454((int64)66, (int64)30);
  total = total + compute455((int64)67, (int64)31);
  total = total + compute456((int64)68, (int64)32);
  total = total + compute457((int64)69, (int64)33);
  total = total + compute458((int64)70, (int64)34);
  total = total + compute459((int64)71, (int64)35);
  total = total + compute460((int64)72, (int64)36);
  total = total + compute461((int64)73, (int64)37);
  total = total + compute462((int64)74, (int64)38);
  total = total + compute463((int64)75, (int64)39);
  total = total + compute464((int64)76, (int64)40);
  total = total + compute465((int64)77, (int64)41);
  total = total + compute466((int64)78, (int64)42);
  total = total + compute467((int64)79, (int64)43);
  total = total + compute468((int64)80, (int64)44);
  total = total + compute469((int64)81, (int64)45);
  total = total + compute470((int64)82, (int64)46);
  total = total + compute471((int64)83, (int64)47);
  total = total + compute472((int64)84, (int64)48);
  total = total + compute473((int64)85, (int64)49);
  total = total + compute474((int64)86, (int64)50);
  total = total + compute475((int64)87, (int64)51);
  total = total + compute476((int64)88, (int64)52);
  total = total + compute477((int64)89, (int64)0);
  total = total + compute478((int64)90, (int64)1);
  total = total + compute479((int64)91, (int64)2);
  total = total + compute480((int64)92, (int64)3);
  total = total + compute481((int64)93, (int64)4);
  total = total + compute482((int64)94, (int64)5);
  total = total + compute483((int64)95, (int64)6);
  total = total + compute484((int64)96, (int64)7);
  total = total + compute485((int64)0, (int64)8);
  total = total + compute486((int64)1, (int64)9);
  total = total + compute487((int64)2, (int64)10);
  total = total + compute488((int64)3, (int64)11);
  total = total + compute489((int64)4, (int64)12);
  total = total + compute490((int64)5, (int64)13);
  total = total + compute491((int64)6, (int64)14);
  total = total + compute492((int64)7, (int64)15);
  total = total + compute493((int64)8, (int64)16);
  total = total + compute494((int64)9, (int64)17);
  total = total + compute495((int64)10, (int64)18);
  total = total + compute496((int64)11, (int64)19);
  total = total + compute497((int64)12, (int64)20);
  total = total + compute498((int64)13, (int64)21);
  total = total + compute499((int64)14, (int64)22);
  total = total + compute500((int64)15, (int64)23);
  total = total + compute501((int64)16, (int64)24);
  total = total + compute502((int64)17, (int64)25);
  total = total + compute503((int64)18, (int64)26);
  total = total + compute504((int64)19, (int64)27);
  total = total + compute505((int64)20, (int64)28);
  total = total + compute506((int64)21, (int64)29);
  total = total + compute507((int64)22, (int64)30);
  total = total + compute508((int64)23, (int64)31);
  total = total + compute509((int64)24, (int64)32);
  total = total + compute510((int64)25, (int64)33);
  total = total + compute511((int64)26, (int64)34);
  total = total + compute512((int64)27, (int64)35);
  total = total + compute513((int64)28, (int64)36);
  total = total + compute514((int64)29, (int64)37);
  total = total + compute515((int64)30, (int64)38);
  total = total + compute516((int64)31, (int64)39);
  total = total + compute517((int64)32, (int64)40);
  total = total + compute518((int64)33, (int64)41);
  total = total + compute519((int64)34, (int64)42);
  return (int32)(total % (int64)256);
}
