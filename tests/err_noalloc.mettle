// `@noalloc` violated directly: the function uses `new`.
struct Point {
    x: int32;
    y: int32;
}
@noalloc fn make_point() -> Point* {
    var p: Point* = new Point;
    return p;
}
fn main() -> int32 {
    var p: Point* = make_point();
    return p->x;
}
