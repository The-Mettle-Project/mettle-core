// M0103: returning the address of a stack local is always a dangling pointer.
fn make_point() -> int32* {
    var values: int32[4];
    values[0] = 7;
    return &values[0];
}

fn main() -> int32 {
    var p: int32* = make_point();
    return p[0];
}
