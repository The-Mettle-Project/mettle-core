// cstring passed to extern function — pointer value preserved
extern fn strlen_c(s: cstring) -> int64 = "strlen";

var MSG: string = "hello";

fn main() -> int32 {
  var len: int64 = strlen_c(MSG.chars);
  if (len == 5) {
    return 1;
  }
  return 0;
}
