// Decorators must come BEFORE `export`, not after it. `export @inline ...` is a
// clear, common ordering mistake and should get a targeted diagnostic.
export @inline fn f() -> int32 {
  return 0;
}

fn main() -> int32 {
  return f();
}
