import "std/io";

fn ok() { puts(cstr("OK\n")); }
fn err() { puts(cstr("ERR\n")); }

fn main() -> int32 {
  if (1) {
    errdefer err();
    defer ok();
    return 2;
  }

  errdefer err();
  defer ok();
  return 0;
}
