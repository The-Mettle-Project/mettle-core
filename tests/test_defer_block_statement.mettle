import "std/io";

fn before() { puts(cstr("BEFORE\n")); }
fn inside() { puts(cstr("INSIDE\n")); }
fn after() { puts(cstr("AFTER\n")); }

fn main() -> int32 {
  before();
  defer {
    inside();
    after();
  }
  return 0;
}
