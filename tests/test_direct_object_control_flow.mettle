fn choose(flag: int32, when_zero: int32, when_nonzero: int32) -> int32 {
  if (flag) {
    return when_nonzero;
  }

  return when_zero;
}

fn main() -> int32 {
  return choose(0, 11, 22);
}
