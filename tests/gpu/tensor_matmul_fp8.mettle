// Exact unscaled FP8 whole-matrix regions. Complete interiors retain direct-MMA
// accumulators across runtime K; every M/N/K edge uses architectural FP8
// conversion and exact cooperative replay.

kernel tensor_matmul_e4m3_e5m2_f32(
    a: uint8*, b: uint8*, c: float32*, d: float32*,
    row_origin: uint32, column_origin: uint32,
    problem_m: uint32, problem_n: uint32, problem_k: uint32,
    lda: uint32, ldb: uint32, ldc: uint32, ldd: uint32) {
  tensor_matmul(
      a, b, c, d, row_origin, column_origin,
      problem_m, problem_n, problem_k,
      shape: m16n16k32,
      a_type: e4m3, b_type: e5m2,
      accumulator_type: f32, result_type: f32,
      a_layout: row, b_layout: col,
      lda: lda, ldb: ldb, ldc: ldc, ldd: ldd);
}

kernel tensor_matmul_e4m3_e5m2_transpose_f32(
    a: uint8*, b: uint8*, c: float32*, d: float32*,
    row_origin: uint32, column_origin: uint32,
    problem_m: uint32, problem_n: uint32, problem_k: uint32) {
  tensor_matmul(
      a, b, c, d, row_origin, column_origin,
      problem_m, problem_n, problem_k,
      shape: m16n16k32,
      a_type: e4m3, b_type: e5m2,
      accumulator_type: f32, result_type: f32,
      a_layout: col, b_layout: row,
      transpose_a: true, transpose_b: true,
      lda: 67, ldb: 71, ldc: 73, ldd: 79);
}
