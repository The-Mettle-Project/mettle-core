trait Addable;
trait SignedNumber;

impl Addable for int32;

fn add_one<T: Addable + SignedNumber>(value: T) -> T {
    return value + 1;
}

fn main() -> int32 {
    return add_one<int32>(41);
}
