// Element-type diagnostic: @simd! over a 64-bit-int array has no kernel, and the
// reason must name the element width (not the generic shape fallback).
fn f(q: int64*, n: int32) -> int32 {
  @simd! for i in 0..n { q[i] = q[i] + 1; }
  return 0;
}
fn main() -> int32 { return 0; }
