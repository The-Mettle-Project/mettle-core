import "std/io";

fn iter_defer() { puts(cstr("ITER_DEFER\n")); }
fn iter_body() { puts(cstr("ITER_BODY\n")); }

fn main() -> int32 {
  var i: int32 = 0;
  while (i < 2) {
    defer iter_defer();
    iter_body();
    i = i + 1;
  }
  return 0;
}
